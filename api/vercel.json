{
    "version": 2,
    "buildCommand": "npm run build",
    "functions": {
        "api/index.js": {
            "maxDuration": 300
        }
    },
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/api/index"
        }
    ]
}
