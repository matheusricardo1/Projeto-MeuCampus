# === Redis (BullMQ job queue) ===
REDIS_URL=redis://localhost:6379
MOODLE_SYNC_QUEUE=moodle-sync
MOODLE_SYNC_WORKER_CONCURRENCY=10

# Must match the same value used by the API — encrypts/decrypts cached Moodle data in Redis
MOODLE_CACHE_ENCRYPTION_KEY=replace-with-a-long-random-secret
